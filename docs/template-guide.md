# Submission Template Guide

This file preserves the hackathon template reference inside the repository. The official guide requires: required metadata in `submission.yaml`; a real problem/solution narrative; actual source code under `src/`; a tested setup guide; demo artifacts; and a presentation. Judges inspect source code and reproducibility, so this submission keeps the implementation, tests and demo assets together.
